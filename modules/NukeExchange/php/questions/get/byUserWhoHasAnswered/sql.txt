SELECT
  Q.user_id,
  Q.userID,
  Q.askedDate,
  Q.title,
  COUNT(A.user_id) As totalAnswers
FROM
  nuke_question Q
LEFT JOIN nuke_answer A on A.questionID = Q.user_id
WHERE A.userID = ?
GROUP BY
  Q.user_id,
  Q.userID,
  Q.askedDate,
  Q.title
