SELECT
  Q.user_id,
  Q.userID,
  Q.askedDate,
  Q.title,
	COALESCE(SUM(A.user_id),0) As totalAnswers
FROM
  nuke_question Q
LEFT JOIN nuke_answer A on A.questionID = Q.user_id
WHERE Q.userID = ?
GROUP BY
  Q.user_id,
  Q.userID,
  Q.askedDate,
  Q.title
